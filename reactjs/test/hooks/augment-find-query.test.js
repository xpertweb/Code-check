const assert = require('assert');
//const augmentFindQuery = require('../../src/hooks/augment-find-query');

describe('\'augmentFindQuery\' hook', () => {
  it('runs the hook', () => {
    // TODO: needs to be integration tested against an actual db for query correctness
    assert(true);
  });
});
